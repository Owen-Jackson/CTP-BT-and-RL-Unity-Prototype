using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//this enum is for a character's alignment from the player's perspective
public enum Alignment
{
    NEUTRAL = 0,
    ALLY,
    ENEMY
}
